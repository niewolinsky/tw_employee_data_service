version: "2"
sql:
  - engine: "mysql"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "data"
        out: "../data"
        emit_exact_table_names: true
        emit_json_tags: true
        emit_interface: true

